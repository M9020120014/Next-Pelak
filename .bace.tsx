
/* --- Base ------------------------------------------------------------------------------------- */
/* --- Lib -------------------------------------------------------------------------------------- */
/* --- Components ------------------------------------------------------------------------------- */
/* --- Types ------------------------------------------------------------------------------------ */
/* --- Data ------------------------------------------------------------------------------------- */
/* --- Constants -------------------------------------------------------------------------------- */
/* --- Functions -------------------------------------------------------------------------------- */

// --- // BUG   | need to be done now
// --- // HACK  | need to be done today
// --- // DOIT  | need to be done soon
// --- // TODO  | need to be done later
// --- // DEBUG | delete this after debugging
// --- // FIX   | maybe a bug or a problem that needs to be fixed
// --- // [ ]   | task is not started yet
// --- // [x]   | task is completed but not tested yet
// --- // XXX   | ◀︎ | ▶︎ | ▲ | ▼ | 🞬 | 🞸 | ● | ⬡ | ⦿ | ← | → | ↑ | ↓ | ↖︎ | ↗︎ | ↘︎ | ↙︎ | ↔︎ | ↕︎ |

/* --- q.:| ♢ |:.p ------------------------------------------------------------------------------ */
/* --- q.:| ♢ |:.p -------------------------------------------------- */
/* --- q.:| ♢ |:.p ------------------ */

// console.log(" --- error | api/auth/[...] | response of fetch ------------------ q.:| ")
// console.log(" --- alert | api/auth/[...] | response of fetch ------------------ o.:| ")
// console.log(" --- debug | api/auth/[...] | response of fetch ------------------ |:./ ") // DEBUG < 

// SubmitErrorsClient(
//   "(auth)/verification",
//   "handleMobileSubmit TRY",
//   error instanceof Error ? error : new Error(String(error))
// );
// SubmitErrorsStringClient(
//   "page/[slug]",
//   "response is not ok",
//   response.statusText
// );
// SubmitErrorsStringServer(
//   "page/[slug]",
//   "response is not ok",
//   response.statusText
// );
// SubmitErrorsServer(
//   "(auth)/verification",
//   "handleMobileSubmit TRY",
//   error instanceof Error ? error : new Error(String(error))
// );
// SubmitErrorsStringServer(
//   "page/[slug]",
//   "response is not ok",
//   response.statusText
// );
// encodeURIComponent(slug)



